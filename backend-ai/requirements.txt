# =============================================================================
# NetLabs AI Backend — Python Dependencies
# =============================================================================
# Install: pip install -r requirements.txt
# =============================================================================

# Flask Web Framework
Flask==3.1.1
python-dotenv==1.1.0

# Google Generative AI (Gemini LLM untuk generate jawaban & soal kuis)
google-generativeai==0.8.5

# Qdrant — Vector Database (pengganti ChromaDB, sesuai Pertemuan 9)
qdrant-client>=1.9.0

# Sentence Transformers — Model Penyematan Lokal + CrossEncoder Re-ranker
sentence-transformers>=3.0.0

# BM25 — Sparse/Lexical Retrieval (Algoritma Okapi BM25 untuk Hybrid Search)
rank-bm25>=0.2.2

# PyMuPDF — Ekstraksi Teks PDF (lebih cepat dari PyPDF2)
PyMuPDF==1.25.4

# LangChain Text Splitter (hanya modul splitter, ringan)
langchain-text-splitters==0.3.8

# Production WSGI Server
gunicorn==23.0.0
