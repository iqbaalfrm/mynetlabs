<![CDATA[@extends('layouts.master')

@section('title', 'Manajemen Kelas - NetLabs Admin')

@section('content')
<div class="nl-page-header d-flex justify-content-between align-items-center">
  <div>
    <h3>Manajemen Kelas</h3>
    <p>Kelola daftar kelas dan penempatan wali kelas.</p>
  </div>
  <button class="btn btn-primary" onclick="openModalCreate()">
    <i class="ti-plus me-1"></i> Tambah Kelas
  </button>
</div>

<div class="row">
  <div class="col-12 grid-margin">
    <div class="card">
      <div class="card-body">
        @if(session('success'))
          <div class="alert alert-success d-flex align-items-center gap-2">
            <i class="ti-check-box"></i> {{ session('success') }}
          </div>
        @endif

        @if(session('error'))
          <div class="alert alert-danger d-flex align-items-center gap-2">
            <i class="ti-alert"></i> {{ session('error') }}
          </div>
        @endif

        <div class="table-responsive">
          <table class="table">
            <thead>
              <tr>
                <th>#</th>
                <th>Nama Kelas</th>
                <th>Wali Kelas</th>
                <th>Jumlah Siswa</th>
                <th class="text-end">Aksi</th>
              </tr>
            </thead>
            <tbody>
              @forelse($kelasList as $kelas)
                <tr>
                  <td>{{ $loop->iteration }}</td>
                  <td>
                    <div class="d-flex align-items-center gap-2">
                      <div style="width:36px;height:36px;border-radius:8px;background:var(--nl-bg);display:flex;align-items:center;justify-content:center;">
                        <i class="ti-headphone-alt" style="color:var(--nl-primary);font-size:16px;"></i>
                      </div>
                      <strong>{{ $kelas->nama_kelas }}</strong>
                    </div>
                  </td>
                  <td>{{ $kelas->waliKelas->nama ?? '-' }}</td>
                  <td>
                    <span class="badge bg-info"><i class="ti-user me-1"></i> {{ $kelas->siswa_count }} siswa</span>
                  </td>
                  <td class="text-end">
                    <button class="btn btn-outline-secondary btn-sm" onclick="openModalEdit({{ $kelas->id }}, '{{ addslashes($kelas->nama_kelas) }}', {{ $kelas->wali_kelas_id ?? 'null' }})">
                      <i class="ti-pencil"></i>
                    </button>
                    <button class="btn btn-outline-danger btn-sm" onclick="deleteKelas({{ $kelas->id }}, '{{ addslashes($kelas->nama_kelas) }}')">
                      <i class="ti-trash"></i>
                    </button>
                  </td>
                </tr>
              @empty
                <tr>
                  <td colspan="5" class="text-center text-muted py-5">
                    <i class="ti-inbox" style="font-size:32px;display:block;margin-bottom:8px;opacity:0.3;"></i>
                    Belum ada kelas.
                  </td>
                </tr>
              @endforelse
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Modal Create/Edit Kelas -->
<div class="modal fade" id="modalKelas" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      <form id="formKelas" method="POST" action="">
        @csrf
        <input type="hidden" name="_method" id="formMethod" value="">
        <div class="modal-header">
          <h5 class="modal-title" id="modalTitle"><i class="ti-plus me-1"></i> Tambah Kelas</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body">
          <div class="mb-3">
            <label class="form-label">Nama Kelas <span class="text-danger">*</span></label>
            <input type="text" name="nama_kelas" id="inputNamaKelas" class="form-control" required placeholder="Contoh: X RPL 1">
          </div>
          <div class="mb-3">
            <label class="form-label">Wali Kelas (Guru)</label>
            <select name="wali_kelas_id" id="inputWaliKelas" class="form-select">
              <option value="">-- Pilih Wali Kelas --</option>
              @foreach($guruList as $guru)
                <option value="{{ $guru->id }}">{{ $guru->nama }} ({{ $guru->username }})</option>
              @endforeach
            </select>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-light" data-bs-dismiss="modal">Batal</button>
          <button type="submit" class="btn btn-primary" id="btnSubmitKelas">Simpan</button>
        </div>
      </form>
    </div>
  </div>
</div>
@endsection

@push('scripts')
<script>
  var modalKelas = new bootstrap.Modal(document.getElementById('modalKelas'));

  function openModalCreate() {
    document.getElementById('modalTitle').innerHTML = '<i class="ti-plus me-1"></i> Tambah Kelas';
    document.getElementById('formKelas').action = '{{ route('admin.kelas.store') }}';
    document.getElementById('formMethod').value = 'POST';
    document.getElementById('inputNamaKelas').value = '';
    document.getElementById('inputWaliKelas').value = '';
    document.getElementById('btnSubmitKelas').textContent = 'Simpan';
    modalKelas.show();
  }

  function openModalEdit(id, nama, waliKelasId) {
    document.getElementById('modalTitle').innerHTML = '<i class="ti-pencil me-1"></i> Edit Kelas';
    document.getElementById('formKelas').action = '{{ url('admin/kelas') }}/' + id;
    document.getElementById('formMethod').value = 'PUT';
    document.getElementById('inputNamaKelas').value = nama;
    document.getElementById('inputWaliKelas').value = waliKelasId || '';
    document.getElementById('btnSubmitKelas').textContent = 'Update';
    modalKelas.show();
  }

  function deleteKelas(id, nama) {
    Swal.fire({
      title: 'Hapus Kelas?',
      text: 'Apakah Anda yakin ingin menghapus kelas "' + nama + '"?',
      icon: 'warning',
      showCancelButton: true,
      confirmButtonColor: '#EF4444',
      cancelButtonColor: '#8B8FA3',
      confirmButtonText: 'Ya, hapus!',
      cancelButtonText: 'Batal'
    }).then((result) => {
      if (result.isConfirmed) {
        fetch('{{ url('admin/kelas') }}/' + id, {
          method: 'DELETE',
          headers: {
            'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]').content,
            'Accept': 'application/json'
          }
        })
        .then(res => res.json())
        .then(data => {
          if (data.success || data.redirect) {
            Swal.fire('Terhapus!', 'Kelas berhasil dihapus.', 'success');
            location.reload();
          } else {
            Swal.fire('Gagal!', data.message || 'Terjadi kesalahan saat menghapus.', 'error');
          }
        })
        .catch(err => {
          Swal.fire('Error!', 'Terjadi kesalahan: ' + err.message, 'error');
        });
      }
    });
  }
</script>
@endpush
]]>